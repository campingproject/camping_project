import styled from 'styled-components';

export const Dialog = styled.dialog`
  position: absolute;
  top: 100%;
  z-index: 1;

  display: block;
  padding: 24px;
  border: 1px solid black;
  border-radius: 24px;
  margin-top: 12px;
`;
